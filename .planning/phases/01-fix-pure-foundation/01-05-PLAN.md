---
phase: 01-fix-pure-foundation
plan: 05
type: execute
wave: 4
gap_closure: true
depends_on:
  - 01-04
files_modified:
  - pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py
  - tests/extensions/tds/frames/legendql_api/test_legendql_api_table_spec_input_frame.py
  - tests/core/request/test_legend_client_e2e.py
autonomous: true
requirements:
  - TEST-01
  - PURE-05
tags:
  - phase-01
  - gap-closure
  - regression-fix
  - legendql-api
  - pure-execution

must_haves:
  truths:
    - "`LegendQLApiTableSpecInputFrame.execute_frame_to_string()` raises `ValueError('Cannot execute frame as its built on top of non-executable input frames: [LegendQLApiTableSpecInputFrame(test_schema.test_table)]')` — NOT `RuntimeError('Expected exactly one LegendQL service/function input frame with project_coordinates, found 0')` (per VERIFICATION.md Gap 1)"
    - "`frame.head(10).execute_frame_to_string()` on a `LegendQLApiTableSpecInputFrame` raises the same `ValueError` with the same message (preserves second assertion block in `test_table_spec_frame_execution_error`)"
    - "`LegendQLApiBaseTdsFrame.execute_frame` continues to route through `LegendClient.execute_pure_string` when the frame tree contains at least one `LegendServiceInputFrameAbstract` or `LegendFunctionInputFrameAbstract` (Plan 04 behavior preserved for executable LegendQL frames)"
    - "`test_e2e_pure_schema_api` and `test_e2e_pure_execute_api` in `tests/core/request/test_legend_client_e2e.py` no longer carry `@pytest.mark.xfail` decorators referencing 'Plan 04' (per VERIFICATION.md Gap 2)"
    - "The two e2e tests pass against a running test server when `JAVA_HOME` is set (they use the depot cascade pattern — `depot_server_host='localhost'`, `depot_server_port=legend_test_server['metadata_port']` — so `LegendClient.get_pure_string_schema`/`execute_pure_string` can fall back to depot-based lookup, which matches the cascade pattern already used by the Plan-04 LegendQL frame tests)"
    - "Plan 04 regression suite continues to pass: `test_legendql_api_legend_person_service_frame_pure_execution_uses_execute_pure_string` and the other `*_pure_execution` tests still pass (the new guard in `execute_frame` only short-circuits when service/function frames are absent)"
  artifacts:
    - path: "pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py"
      provides: "Guarded `execute_frame` override that delegates to `BaseTdsFrame.execute_frame` (SQL path -> `get_legend_client()` -> `ValueError`) when no service/function ancestor is present"
      contains: "BaseTdsFrame.execute_frame(self, result_handler, chunk_size)"
    - path: "tests/core/request/test_legend_client_e2e.py"
      provides: "Removed `@pytest.mark.xfail` decorators from `test_e2e_pure_schema_api` and `test_e2e_pure_execute_api`; tests now use `depot_server_host`/`depot_server_port` so the depot cascade fixes the schema/execute path"
      contains: "depot_server_host=\"localhost\""
  key_links:
    - from: "LegendQLApiBaseTdsFrame.execute_frame"
      to: "BaseTdsFrame.execute_frame"
      via: "super() delegation when no LegendService/LegendFunction frame is present"
      pattern: "BaseTdsFrame\\.execute_frame\\(self, result_handler"
    - from: "test_e2e_pure_schema_api"
      to: "legend_test_server[\"metadata_port\"]"
      via: "depot_server_port constructor argument on LegendClient"
      pattern: "depot_server_port=legend_test_server\\[\"metadata_port\"\\]"
    - from: "test_e2e_pure_execute_api"
      to: "legend_test_server[\"metadata_port\"]"
      via: "depot_server_port constructor argument on LegendClient"
      pattern: "depot_server_port=legend_test_server\\[\"metadata_port\"\\]"
---

<objective>
Close two verification gaps from `01-VERIFICATION.md` introduced in Plan 04:

**Gap 1 (BLOCKER, TEST-01):** Fix the regression in `LegendQLApiBaseTdsFrame.execute_frame` that breaks `test_table_spec_frame_execution_error`. The override unconditionally calls `_get_legendql_input_project_coordinates()`, which raises `RuntimeError` for non-service/non-function frames (TableSpec, CSV, etc.). Guard the override: when `get_all_tds_frames()` contains zero `LegendServiceInputFrameAbstract`/`LegendFunctionInputFrameAbstract` instances, delegate to `BaseTdsFrame.execute_frame(self, result_handler, chunk_size)` (the SQL path), which calls `get_legend_client()` and raises the expected `ValueError("Cannot execute frame as its built on top of non-executable input frames: [...]")`.

**Gap 2 (Warning, PURE-05):** Remove the stale `@pytest.mark.xfail` decorators (referencing "Plan 04 once the correct service Pure root expression is confirmed") from `test_e2e_pure_schema_api` and `test_e2e_pure_execute_api` in `tests/core/request/test_legend_client_e2e.py`. Plan 04 is complete and the Pure expression form was resolved. Update both tests to construct `LegendClient` with the depot cascade pattern (`depot_server_host="localhost"`, `depot_server_port=legend_test_server["metadata_port"]`) so the schema/execute path uses the same depot-aware mechanism that Plan 04's LegendQL frame tests already exercise.

Purpose: Reach phase-complete `verified` status by closing 2/4 outstanding gaps. Without this plan, Phase 1's TEST-01 (PCT remains green) requirement is broken by Plan 04's own change, and PURE-05's evidence path through `test_legend_client_e2e.py` is masked by stale xfails.

Output: One guarded `execute_frame` override (3-5 added lines); two xfail decorators removed; two test bodies updated to pass `depot_server_host`/`depot_server_port`; full Phase 1 regression suite green.
</objective>

<execution_context>
@/Users/deepyaman/github/finos/pylegend/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deepyaman/github/finos/pylegend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-pure-foundation/01-CONTEXT.md
@.planning/phases/01-fix-pure-foundation/01-VERIFICATION.md
@.planning/phases/01-fix-pure-foundation/01-04-SUMMARY.md
@.planning/phases/01-fix-pure-foundation/01-03-SUMMARY.md
@pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py
@pylegend/core/tds/abstract/frames/base_tds_frame.py
@pylegend/core/tds/abstract/frames/input_tds_frame.py
@pylegend/core/tds/legendql_api/frames/legendql_api_input_tds_frame.py
@pylegend/extensions/tds/abstract/legend_service_input_frame.py
@pylegend/extensions/tds/abstract/legend_function_input_frame.py
@pylegend/extensions/tds/legendql_api/frames/legendql_api_table_spec_input_frame.py
@pylegend/core/request/legend_client.py
@tests/extensions/tds/frames/legendql_api/test_legendql_api_table_spec_input_frame.py
@tests/extensions/tds/frames/legendql_api/test_legendql_api_legend_service_frame.py
@tests/core/request/test_legend_client_e2e.py
@tests/conftest.py
</context>

<phase_artifacts>
## Artifacts this phase produces

Artifacts created or modified by THIS plan (Wave 4, Plan 05 — gap closure):

- Modified `LegendQLApiBaseTdsFrame.execute_frame` in `pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py`:
  - New guard at the top of the method body: scan `self.get_all_tds_frames()` for instances of `LegendServiceInputFrameAbstract` or `LegendFunctionInputFrameAbstract`. If none are found, delegate by calling `BaseTdsFrame.execute_frame(self, result_handler, chunk_size)` (explicit base-class call, NOT `super().execute_frame(...)` — the MRO would otherwise loop back through `LegendQLApiBaseTdsFrame`).
  - Required local imports inside the method body (to avoid module-level cyclic imports): `from pylegend.extensions.tds.abstract.legend_service_input_frame import LegendServiceInputFrameAbstract`, `from pylegend.extensions.tds.abstract.legend_function_input_frame import LegendFunctionInputFrameAbstract`. Note: `BaseTdsFrame` is already imported at module level (line 43); no new module-level import needed.
- Modified `tests/core/request/test_legend_client_e2e.py`:
  - `test_e2e_pure_schema_api`: removed `@pytest.mark.xfail(...)` decorator (kept `@pytest.mark.skipif(os.environ.get("JAVA_HOME") is None, ...)`); updated `LegendClient(...)` constructor to add `depot_server_host="localhost"` and `depot_server_port=legend_test_server["metadata_port"]`.
  - `test_e2e_pure_execute_api`: same — removed `@pytest.mark.xfail(...)`; updated `LegendClient(...)` to add depot host/port.

Cross-plan symbols (unchanged, referenced from other plans):

- `LegendServiceInputFrameAbstract`, `LegendFunctionInputFrameAbstract` [Plan 02 — now used as `isinstance` guard targets, no modification needed]
- `BaseTdsFrame.execute_frame` [pre-existing — unchanged; called explicitly via base-class call in the guard branch]
- `LegendClient.get_pure_string_schema`, `LegendClient.execute_pure_string`, `LegendClient.__depot_server_host`, `LegendClient.__depot_server_port`, depot fallback path inside both methods [Plan 03 — already implemented; this plan only adds new constructor kwargs at the call site]
</phase_artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard LegendQLApiBaseTdsFrame.execute_frame to delegate to BaseTdsFrame.execute_frame when no service/function input frame is present</name>
  <files>
    pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py,
    tests/extensions/tds/frames/legendql_api/test_legendql_api_table_spec_input_frame.py
  </files>
  <read_first>
    - pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py (current `execute_frame` at lines 63-72 — unconditionally calls `_get_legendql_input_project_coordinates()` which raises `RuntimeError('found 0')` for non-service frames)
    - pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py (current `_get_legendql_input_project_coordinates` at lines 74-90 — keep this helper untouched; only the calling site changes)
    - pylegend/core/tds/abstract/frames/base_tds_frame.py (`BaseTdsFrame.execute_frame` at lines 106-112 — the SQL fallback path; calls `self.get_legend_client()` -> `self.execute_sql_string(self.to_sql_query(), ...)`. The `get_legend_client()` method at lines 79-104 raises `ValueError("Cannot execute frame as its built on top of non-executable input frames: [...]")` when any `InputTdsFrame` is not an `ExecutableInputTdsFrame`.)
    - pylegend/core/tds/abstract/frames/input_tds_frame.py (`InputTdsFrame`, `ExecutableInputTdsFrame`, `NonExecutableInputTdsFrame` hierarchy)
    - pylegend/core/tds/legendql_api/frames/legendql_api_input_tds_frame.py (`LegendQLApiNonExecutableInputTdsFrame` at line 47 — base for TableSpec/CSV LegendQL frames)
    - pylegend/extensions/tds/legendql_api/frames/legendql_api_table_spec_input_frame.py (concrete `LegendQLApiTableSpecInputFrame` to confirm the class lineage: `LegendQLApiNonExecutableInputTdsFrame -> LegendQLApiInputTdsFrame -> LegendQLApiBaseTdsFrame`)
    - pylegend/extensions/tds/abstract/legend_service_input_frame.py (`LegendServiceInputFrameAbstract` — used as positive isinstance check)
    - pylegend/extensions/tds/abstract/legend_function_input_frame.py (`LegendFunctionInputFrameAbstract` — used as positive isinstance check)
    - tests/extensions/tds/frames/legendql_api/test_legendql_api_table_spec_input_frame.py (`test_table_spec_frame_execution_error` at lines 38-54 — asserts BOTH `frame.execute_frame_to_string()` AND `frame.head(10).execute_frame_to_string()` raise `ValueError("Cannot execute frame as its built on top of non-executable input frames: [LegendQLApiTableSpecInputFrame(test_schema.test_table)]")`)
    - .planning/phases/01-fix-pure-foundation/01-VERIFICATION.md (Gap 1 description at lines 7-35 — confirms the missing guard logic)
    - .planning/phases/01-fix-pure-foundation/01-04-PLAN.md (Plan 04 context — note this file is COMPLETE; do not modify it. The override added there must be kept and only refined.)
  </read_first>
  <behavior>
    - Calling `LegendQLApiTableSpecInputFrame(['test_schema', 'test_table'], [PrimitiveTdsColumn.integer_column("col1"), PrimitiveTdsColumn.string_column("col2")]).execute_frame_to_string()` raises `ValueError` with message exactly `"Cannot execute frame as its built on top of non-executable input frames: [LegendQLApiTableSpecInputFrame(test_schema.test_table)]"`. The RuntimeError path is NOT taken.
    - Calling `frame.head(10).execute_frame_to_string()` on the same TableSpec frame raises the same `ValueError` with the same message.
    - Calling `.execute_frame_to_string()` on any frame whose tree contains at least one `LegendServiceInputFrameAbstract` or `LegendFunctionInputFrameAbstract` STILL routes through `LegendClient.execute_pure_string` (Plan 04 behavior preserved). Specifically, `test_legendql_api_legend_person_service_frame_pure_execution_uses_execute_pure_string` continues to pass.
    - `LegendQLApiBaseTdsFrame._get_legendql_input_project_coordinates` is unchanged (still raises `RuntimeError("found N")` when called with N != 1 — the helper is now reachable only from the positive branch, where N >= 1 is guaranteed by the guard; an N > 1 case still raises as before, preserving the multi-project-coordinates spoofing guard from T-04-02 in the Plan 04 threat model).
    - `BaseTdsFrame.execute_frame` at `pylegend/core/tds/abstract/frames/base_tds_frame.py` is NOT modified (preserves SQL path for Legacy/Pandas frames; per CONTEXT.md D-07 + RESEARCH.md Pitfall 6).
    - `test_table_spec_frame_creation` and `test_table_spec_frame_creation_duplicated_columns_error` (the other two tests in the same file) continue to pass unchanged.
  </behavior>
  <action>
    Implements TEST-01 gap fix (PCT regression introduced by Plan 04).

    1. `pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py`:
       - Modify the existing `execute_frame` method (currently lines 63-72). Insert a guard at the START of the method body, BEFORE the existing call to `self._get_legendql_input_project_coordinates()`. The guard:
         - Imports `LegendServiceInputFrameAbstract` and `LegendFunctionInputFrameAbstract` locally inside the method body (parallel pattern with `_get_legendql_input_project_coordinates` which already does local imports to avoid cyclic-import risk).
         - Iterates `self.get_all_tds_frames()` and returns `True` if any frame `isinstance` matches either abstract; returns `False` otherwise. Use a generator expression with `any(...)` for conciseness.
         - When the guard returns `False`: call `BaseTdsFrame.execute_frame(self, result_handler, chunk_size)` and return its result. Use an EXPLICIT base-class call `BaseTdsFrame.execute_frame(self, ...)` — DO NOT use `super().execute_frame(...)`. Reason: `LegendQLApiBaseTdsFrame` inherits from both `LegendQLApiTdsFrame` and `BaseTdsFrame`; `super()` would resolve via MRO to `LegendQLApiTdsFrame` first, which does not define `execute_frame`. The explicit base-class call is the deterministic path to the SQL implementation that calls `get_legend_client()` -> raises `ValueError`.
         - When the guard returns `True`: take the existing Pure-execution path (call `_get_legendql_input_project_coordinates()`, then `self.get_legend_client().execute_pure_string(...)`, then `result_handler.handle_result(...)`). This branch's body is unchanged from Plan 04.
       - DO NOT modify `_get_legendql_input_project_coordinates` (lines 74-90); its semantics remain the same.
       - DO NOT modify the `head`, `limit`, `filter`, etc. method implementations later in the file.
       - DO NOT add a module-level import for `LegendServiceInputFrameAbstract`/`LegendFunctionInputFrameAbstract` — they MUST be imported inside the method body to mirror the existing local-import pattern in `_get_legendql_input_project_coordinates` (avoids any cyclic-import risk that could be triggered when the abstract modules eventually import LegendQL frames).
       - Preserve Apache 2.0 header, `__all__`, mypy strict, flake8 max-line-length 127.
       - Suggested method body shape (prose only, NOT a code fence): three logical steps in order — (a) local imports of the two abstracts; (b) `has_legendql_input = any(isinstance(frame, (LegendServiceInputFrameAbstract, LegendFunctionInputFrameAbstract)) for frame in self.get_all_tds_frames())`; (c) `if not has_legendql_input: return BaseTdsFrame.execute_frame(self, result_handler, chunk_size)`; then the original Plan 04 body unchanged.

    2. `tests/extensions/tds/frames/legendql_api/test_legendql_api_table_spec_input_frame.py`:
       - Do NOT modify the test file content (the assertions in `test_table_spec_frame_execution_error` are correct as written and must keep passing). Touching this file in `files_modified` is to confirm the file's behavior is the acceptance gate; the file content stays byte-identical. Verification re-runs the test to confirm pass.
  </action>
  <verify>
    <automated>uv run mypy pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py --config-file .github/workflows/typing/config.cfg &amp;&amp; uv run flake8 pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py &amp;&amp; uv run pytest tests/extensions/tds/frames/legendql_api/test_legendql_api_table_spec_input_frame.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BaseTdsFrame.execute_frame(self, result_handler, chunk_size)" pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py` returns 1 (the explicit base-class delegation line)
    - `grep -c "isinstance(frame, (LegendServiceInputFrameAbstract, LegendFunctionInputFrameAbstract))" pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py` returns at least 1 (guard isinstance check, in addition to the existing one in `_get_legendql_input_project_coordinates`)
    - `grep -c "def execute_frame(" pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py` returns 1 (still one override on the LegendQL base)
    - `grep -c "def _get_legendql_input_project_coordinates" pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py` returns 1 (helper preserved)
    - `grep -c "execute_pure_string" pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py` returns 1 (Pure execution branch preserved)
    - `grep -c "def execute_frame(" pylegend/core/tds/abstract/frames/base_tds_frame.py` returns 1 (BaseTdsFrame SQL path NOT modified)
    - `git diff --stat pylegend/core/tds/abstract/frames/base_tds_frame.py` reports zero changes (file untouched)
    - `git diff --stat tests/extensions/tds/frames/legendql_api/test_legendql_api_table_spec_input_frame.py` reports zero changes (test file untouched — the test passes as-is once the source fix lands)
    - `uv run mypy pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py --config-file .github/workflows/typing/config.cfg` exits 0
    - `uv run flake8 pylegend/core/tds/legendql_api/frames/legendql_api_base_tds_frame.py` exits 0
    - `uv run pytest tests/extensions/tds/frames/legendql_api/test_legendql_api_table_spec_input_frame.py -x -q` exits 0 (all three tests pass — `test_table_spec_frame_creation`, `test_table_spec_frame_execution_error`, `test_table_spec_frame_creation_duplicated_columns_error`)
    - `uv run pytest tests/extensions/tds/frames/legendql_api/test_legendql_api_table_spec_input_frame.py::TestLegendQLApiTableSpecInputFrame::test_table_spec_frame_execution_error -x -q` exits 0 (the specific failing test now passes)
  </acceptance_criteria>
  <done>The guard short-circuits to `BaseTdsFrame.execute_frame` for non-service/non-function LegendQL frames; `test_table_spec_frame_execution_error` passes with the expected `ValueError` (not the previous `RuntimeError`); Pure execution path preserved for LegendQL service/function frames; `BaseTdsFrame.execute_frame` source unchanged; static checks clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove stale @pytest.mark.xfail decorators from test_e2e_pure_schema_api and test_e2e_pure_execute_api; wire both to use the depot cascade fixture pattern</name>
  <files>
    tests/core/request/test_legend_client_e2e.py
  </files>
  <read_first>
    - tests/core/request/test_legend_client_e2e.py (current state — `test_e2e_pure_schema_api` at lines 118-132 and `test_e2e_pure_execute_api` at lines 134-149 each carry both `@pytest.mark.skipif(JAVA_HOME)` AND `@pytest.mark.xfail(reason="...To be resolved in Plan 04...")`)
    - tests/conftest.py (`legend_test_server` fixture at line ~42 — yields `{"engine_port": engine_port, "metadata_port": metadata_port}`. The `metadata_port` is the depot server port; `MetadataServerHandler` at line ~64 serves the PMCD at the depot path `/depot/api/projects/org.finos.legend.pylegend/pylegend-test-models/versions/...`.)
    - pylegend/core/request/legend_client.py (`LegendClient.__init__` at lines 50-70 accepts optional `depot_server_host` and `depot_server_port`; `get_pure_string_schema` at lines 100-152 and `execute_pure_string` at lines 154-191 BOTH have a depot fallback branch — they try Pure first, and on `RuntimeError` fall back to building a `_build_depot_execute_input` and re-issue `generatePlan`/`execute`. The fallback only triggers when `__depot_server_host` and `__depot_server_port` are both set; otherwise they fall back to SQL.)
    - tests/extensions/tds/frames/legendql_api/test_legendql_api_legend_service_frame.py (lines 170-280 — Plan 04's Pure-path tests; all construct `LegendClient` with `depot_server_host="localhost", depot_server_port=legend_test_server["metadata_port"]`. This is the canonical cascade pattern this task adopts.)
    - .planning/phases/01-fix-pure-foundation/01-VERIFICATION.md (Gap 2 at lines 36-59 — confirms xfail removal is the required fix)
    - .planning/phases/01-fix-pure-foundation/01-03-SUMMARY.md (notes the xfail was added pending Plan 04; Plan 04 is complete — removal is now correct)
    - .planning/phases/01-fix-pure-foundation/01-04-SUMMARY.md (records that the Pure expression form resolved and Plan-04 LegendQL frame tests pass via the depot cascade)
  </read_first>
  <behavior>
    - `test_e2e_pure_schema_api` no longer has `@pytest.mark.xfail` and runs as a normal (skip-if-no-Java) test. When `JAVA_HOME` is set and `legend_test_server` is up, it issues HTTP calls to `pure/v1/grammar/grammarToJson/lambda` and `pure/v1/execution/generatePlan`. The depot fallback (if triggered by an engine-side `NullPointerException` on the `.all()` form) uses the metadata server's `/depot/api/projects/...` endpoint. The final assertion `", ".join([str(x) for x in res]) == "TdsColumn(Name: First Name, ...)"` passes.
    - `test_e2e_pure_execute_api` no longer has `@pytest.mark.xfail` and runs the same way. The final assertions on `parsed["result"]["columns"]`, `parsed["result"]["rows"][0]["values"]`, and `len(parsed["result"]["rows"]) == 7` all pass.
    - The remaining four tests in the same file (`test_e2e_schema_string_api`, `test_e2e_execute_string_api`, `test_e2e_parse_model_api`, `test_e2e_compile_api`) are unchanged and continue to pass.
    - When `JAVA_HOME` is NOT set, both tests are SKIPPED (not failed, not xpassed) — the `@pytest.mark.skipif(os.environ.get("JAVA_HOME") is None, ...)` decorator is preserved.
    - `pytest tests/core/request/test_legend_client_e2e.py -v` shows zero xfails for the file (Plan 03 produced 4 passed + 2 xfailed; this plan produces 6 passed when `JAVA_HOME` is set, or 4 passed + 2 skipped when it is not).
    - No regression in any other test file under `tests/`.
  </behavior>
  <action>
    Implements PURE-05 evidence gap fix (Gap 2 from VERIFICATION.md).

    1. `tests/core/request/test_legend_client_e2e.py`:
       - On `test_e2e_pure_schema_api` (currently lines 118-132):
         - DELETE the entire `@pytest.mark.xfail(...)` decorator block (lines 119-123 — three lines including the multi-line `reason="..."`). The decorator spans `@pytest.mark.xfail(\n        reason="...\n               "...\n               "..."\n    )` — remove all four lines.
         - KEEP the `@pytest.mark.skipif(os.environ.get("JAVA_HOME") is None, reason="JAVA_HOME unset; requires legend_test_server")` decorator immediately above it (line 118) untouched.
         - In the test body, modify the `LegendClient(...)` constructor call (currently `LegendClient("localhost", legend_test_server["engine_port"], secure_http=False)` at line 125) to add two new keyword arguments: `depot_server_host="localhost"` and `depot_server_port=legend_test_server["metadata_port"]`. The resulting constructor call must be EXACTLY of the same form used in `test_legendql_api_legend_person_service_frame_pure_gen` (see read_first reference). The five-line form is preferred for flake8 line-length compliance, e.g., one keyword per line continuation if needed.
         - Do NOT change the assertion at the end of the test — it must remain `assert ", ".join([str(x) for x in res]) == "TdsColumn(Name: First Name, Type: String), TdsColumn(Name: Last Name, Type: String), TdsColumn(Name: Age, Type: Integer), TdsColumn(Name: Firm/Legal Name, Type: String)"`.
       - On `test_e2e_pure_execute_api` (currently lines 134-149):
         - DELETE the `@pytest.mark.xfail(...)` decorator block (lines 135-139 — same pattern as above; four lines).
         - KEEP the `@pytest.mark.skipif(...)` decorator on the line immediately above (line 134) untouched.
         - In the test body, modify the `LegendClient(...)` constructor call (currently at line 141) to add `depot_server_host="localhost"` and `depot_server_port=legend_test_server["metadata_port"]`. Same form as above.
         - Do NOT change the assertions at the end (`parsed["result"]["columns"] == ["First Name", ...]`, `parsed["result"]["rows"][0]["values"] == ["Peter", "Smith", 23, "Firm X"]`, `len(parsed["result"]["rows"]) == 7`).
       - Do NOT touch the other four test methods in the file (`test_e2e_schema_string_api`, `test_e2e_execute_string_api`, `test_e2e_parse_model_api`, `test_e2e_compile_api`).
       - Preserve module-level imports (`import json`, `import os`, `import pytest`, `from pylegend.core.request.legend_client import LegendClient`, `from pylegend.core.project_cooridnates import VersionedProjectCoordinates`, `from pylegend._typing import PyLegendDict, PyLegendUnion`) — no new imports required.
       - Preserve the Apache 2.0 copyright header and `flake8` max-line-length 127. Mypy strict must remain green.
  </action>
  <verify>
    <automated>uv run flake8 tests/core/request/test_legend_client_e2e.py &amp;&amp; uv run pytest tests/core/request/test_legend_client_e2e.py -q --collect-only 2&gt;&amp;1 | grep -E "test_e2e_pure_(schema|execute)_api"</automated>
    <human-check>
      End-of-phase verification (workflow.human_verify_mode = end-of-phase):

      1. Run the regression-relevant subset locally: `JAVA_HOME=/Users/deepyaman/Library/Caches/rattler/cache/pkgs/openjdk-17.0.17-h99a4030_0/lib/jvm uv run pytest tests/core/request/test_legend_client_e2e.py tests/extensions/tds/frames/legendql_api/ -q`. Expected: 0 failures, 0 unexpected xpasses; `test_e2e_pure_schema_api` and `test_e2e_pure_execute_api` both PASS (or SKIP if `JAVA_HOME` is unset). All Plan 04 LegendQL service/function Pure tests continue to pass.
      2. Run the full repository suite: `JAVA_HOME=... uv run pytest tests/ -q`. Expected: 0 failures; new pass count matches Plan 04 baseline + 2 (the two e2e tests previously xfail-marked).
      3. Confirm `git diff tests/core/request/test_legend_client_e2e.py` shows: two `@pytest.mark.xfail(...)` blocks deleted and two `LegendClient(...)` constructor calls extended with `depot_server_host`/`depot_server_port`. No other changes.
      4. Confirm `git grep -n "To be resolved in Plan 04" tests/` returns zero results (stale reason text fully removed).
      5. Confirm `pyproject.toml` and `uv.lock` show zero diff from Phase 1 baseline.

      Reply with `approved: pytest <N> passed` OR `issues: <description>` so the orchestrator can decide whether to re-run `/gsd-verify-phase 01`.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "To be resolved in Plan 04" tests/core/request/test_legend_client_e2e.py` returns 0 (stale reason text removed)
    - `grep -c "@pytest.mark.xfail" tests/core/request/test_legend_client_e2e.py` returns 0 (both xfail decorators removed)
    - `grep -c "@pytest.mark.skipif" tests/core/request/test_legend_client_e2e.py` returns at least 2 (both skipif decorators preserved)
    - `grep -c "def test_e2e_pure_schema_api" tests/core/request/test_legend_client_e2e.py` returns 1 (test still present)
    - `grep -c "def test_e2e_pure_execute_api" tests/core/request/test_legend_client_e2e.py` returns 1 (test still present)
    - `grep -c "depot_server_host=\"localhost\"" tests/core/request/test_legend_client_e2e.py` returns at least 2 (added to both modified tests)
    - `grep -c "depot_server_port=legend_test_server\[\"metadata_port\"\]" tests/core/request/test_legend_client_e2e.py` returns at least 2 (added to both modified tests)
    - `grep -c "def test_e2e_schema_string_api\|def test_e2e_execute_string_api\|def test_e2e_parse_model_api\|def test_e2e_compile_api" tests/core/request/test_legend_client_e2e.py` returns 4 (other tests preserved)
    - `uv run flake8 tests/core/request/test_legend_client_e2e.py` exits 0
    - `uv run pytest tests/core/request/test_legend_client_e2e.py --collect-only -q` lists exactly 6 tests (no collection errors)
    - `git diff --stat pyproject.toml uv.lock` reports zero changes (no dependency drift in Phase 1)
  </acceptance_criteria>
  <done>Both `test_e2e_pure_*` tests no longer carry `@pytest.mark.xfail`; both construct `LegendClient` with the depot cascade kwargs; collection succeeds; flake8 green; the four unrelated e2e tests are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `LegendQLApiBaseTdsFrame.execute_frame` -> `BaseTdsFrame.execute_frame` | New explicit base-class delegation when the frame tree lacks a service/function ancestor. Same trust posture as the pre-existing `BaseTdsFrame` SQL path. |
| `LegendClient.execute_pure_string` / `get_pure_string_schema` -> depot HTTP | Depot fallback already implemented in Plan 03; this plan only adds the constructor wiring at the test call site. Same boundary as the Plan 04 frame tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | The new guard in `execute_frame` could be circumvented if a future subclass of `LegendQLApiBaseTdsFrame` overrides `get_all_tds_frames()` to hide service/function frames | mitigate | The guard uses the same `get_all_tds_frames()` API that `_get_legendql_input_project_coordinates` already relies on AND that `BaseTdsFrame.get_legend_client()` relies on for the SQL-path error message — any subclass that lies about its tree breaks all three. Acceptance criterion grep-asserts the helper is unchanged, so the contract surface is single-source. |
| T-05-02 | Tampering | Removing xfail decorators could surface a newly-flaky test if the engine's `.all()` parsing regresses | accept | The depot cascade path implemented in Plan 03 is the documented fallback (RESEARCH.md). If `.all()` regresses in the engine, the depot fallback re-issues against the precomputed PMCD; flakiness is a Legend-engine bug, not a PyLegend bug. Mitigation is acceptable because the human-check step (end-of-phase) confirms both tests pass against the real fixture. |
| T-05-03 | Repudiation | The new `BaseTdsFrame.execute_frame` delegation may attempt to call `self.to_sql_query()` on a frame whose `to_sql_query_object()` raises | accept | Same posture as before Plan 04. `LegendQLApiTableSpecInputFrame.to_sql_query()` is already exercised by `test_table_spec_frame_creation` and is known to work; the delegation only reaches `to_sql_query()` if `get_legend_client()` does NOT raise first. For TableSpec frames, `get_legend_client()` raises `ValueError` BEFORE the SQL is generated — `to_sql_query()` is not reached. This is the test's expected path. |
| T-05-04 | Denial of Service | The guard adds an O(n) scan over `get_all_tds_frames()` for every `execute_frame` call | accept | Frame trees are bounded by user-built query depth (typically 5-30 frames). Cost is negligible relative to network I/O. Same iteration cost as the existing helper. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no new package installs. Uses existing `pytest`, `requests`. |
</threat_model>

<verification>
- `test_table_spec_frame_execution_error` PASSES (was the BLOCKER failure in VERIFICATION.md Gap 1).
- All three tests in `test_legendql_api_table_spec_input_frame.py` PASS.
- All Plan 04 LegendQL service/function Pure-path tests still PASS (Pure execution path preserved).
- `test_e2e_pure_schema_api` and `test_e2e_pure_execute_api` no longer carry `@pytest.mark.xfail`; both PASS when `JAVA_HOME` is set (end-of-phase human-check verifies).
- `BaseTdsFrame.execute_frame` source is UNCHANGED (Legacy/Pandas SQL path preserved per D-07 + RESEARCH.md Pitfall 6).
- Phase 1 introduces zero new Python dependencies (`pyproject.toml`/`uv.lock` diff = 0).
- Full Phase 1 regression suite green (TEST-01 PCT guard satisfied; TEST-02 LegendQL integration guard satisfied).
</verification>

<success_criteria>
- VERIFICATION.md Gap 1 (BLOCKER) CLOSED: `test_table_spec_frame_execution_error` passes with the expected `ValueError`.
- VERIFICATION.md Gap 2 (Warning) CLOSED: stale `@pytest.mark.xfail` decorators removed from the two `test_e2e_pure_*` tests; tests pass against the running server via the depot cascade.
- TEST-01 ack: PCT-exercised `test_table_spec_frame_execution_error` passes; all other Phase 1 PCT regression tests remain green (CSV input frame, applied-function suite, etc.).
- PURE-05 ack: e2e evidence path through `test_legend_client_e2e.py::test_e2e_pure_schema_api` and `test_e2e_pure_execute_api` is no longer masked by xfail; the Pure expression form for services is documented as working end-to-end.
- D-07 ack: Legacy and Pandas API frame execution paths remain unchanged (`BaseTdsFrame.execute_frame` source untouched; `git diff --stat` reports zero changes on the file).
- Re-verification by `/gsd-verify-phase 01` should return `status: verified` with 4/4 truths satisfied (Truth 4 unblocked; Truth 3 promoted from PARTIAL to VERIFIED once the e2e tests pass).
</success_criteria>

<output>
Create `.planning/phases/01-fix-pure-foundation/01-05-SUMMARY.md` when done. Record:
- Exact diff stats for the two modified source files (`legendql_api_base_tds_frame.py`, `test_legend_client_e2e.py`) — lines added / removed
- Output of `uv run pytest tests/extensions/tds/frames/legendql_api/test_legendql_api_table_spec_input_frame.py -v` (must show all 3 tests passing, including `test_table_spec_frame_execution_error`)
- Output of `JAVA_HOME=... uv run pytest tests/core/request/test_legend_client_e2e.py -v` (must show 6 tests, 0 xfail, 0 xpass; if `JAVA_HOME` unset, must show 4 passed + 2 skipped)
- Output of `JAVA_HOME=... uv run pytest tests/ -q` summary line (total passed / failed / skipped / xfailed)
- Confirmation: `git diff --stat pylegend/core/tds/abstract/frames/base_tds_frame.py` is empty (BaseTdsFrame unchanged)
- Confirmation: `grep -c "To be resolved in Plan 04" tests/` is 0 (stale reason text removed everywhere)
- Human-check resume signal from Task 2's `<human-check>` block
- Whether re-running `/gsd-verify-phase 01` reports `status: verified` (target: yes)
</output>
